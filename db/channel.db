CREATE TABLE def_linux (
  id      INTEGER PRIMARY KEY AUTOINCREMENT,
  value   TEXT UNIQUE,
  meaning TEXT NOT NULL,
  author  TEXT NOT NULL,
  locked  INTEGER DEFAULT 0,
  active  INTEGER DEFAULT 1,
  date    TEXT DEFAULT '0000-00-00',
  hits    INTEGER DEFAULT 0,
  link    INTEGER
);
